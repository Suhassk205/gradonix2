"use client";

export const FeatureTitle = ({ text }: { text: string }) => {
  return (
    <>
      <h2>{text}</h2>
    </>
  );
};
export const FeatureDescription = ({ text }: { text: string }) => {
  return (
    <>
      <p>{text}</p>
    </>
  );
};
